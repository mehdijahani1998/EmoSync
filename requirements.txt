# Requirements for EmoSync Local Pipeline (local_pipeline.py)
# Run: pip install -r requirements.txt
# Recommended: use the penv/ virtual environment

# Web server
fastapi>=0.110.0
uvicorn>=0.29.0
python-multipart>=0.0.9

# Video processing
opencv-python>=4.9.0.80

# Deep learning / MPS support
torch>=2.2.0

# Speech transcription
omegaconf>=2.3.0   # Needed to patch torch.load safe globals for pyannote VAD (PyTorch 2.6+)
whisperx>=3.1.5

# Facial emotion analysis
deepface>=0.0.93
tf-keras>=2.16.0  # Required by deepface/retinaface when TensorFlow >= 2.16

# Local LLM via Ollama
ollama>=0.2.0
